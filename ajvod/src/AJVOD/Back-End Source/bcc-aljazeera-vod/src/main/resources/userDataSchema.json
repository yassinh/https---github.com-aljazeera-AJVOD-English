{
    fields: {
        'bcc.language': {
            type: 'string',
            writeAccess: 'serverOnly',
            format:"regex('^[a-z]{2}$')"
        },
        'bcc.locale': {
            type: 'string',
            writeAccess: 'serverOnly',
            format:"regex('^[a-z]{2}_[a-z]{2}$')"
        },
        'bcc.subscriptions.name': {
            type: 'string',
            writeAccess: 'clientModify'
        }
    },
    dynamicSchema: false
}
