<!--
  The selected channel should be given the class is-selected.
  Don't follow my hacky logic here.
-->
<li class="theme-orange{@if cond="'{title}'.indexOf('English') > -1"} is-selected{/if}">
  <a href="#">
    <img src="images/logo-trans.png" alt="{title}">
    <p class="title">{title}</p>
  </a>
</li>
